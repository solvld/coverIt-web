import { GenerateTrackForm } from 'features/generate'
import { StyledPage } from 'shared/ui/StyledPage'

const Page = () => {
  return (
    <StyledPage>
      <GenerateTrackForm />
    </StyledPage>
  )
}

export default Page
